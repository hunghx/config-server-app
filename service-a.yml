spring:
  application:
        name: service-a
  config:
    import: optional:configserver:http://localhost:8888/
app:
    title: service-a
    username: user
    pass: 123456
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: refresh